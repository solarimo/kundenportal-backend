import { Router, Response, Request } from 'express';

const router = Router();

router.post(
  '/register/calculate',
  (
    req: Request,
    res: Response
  ) => {

  }
)


export { router as calculateRouter };